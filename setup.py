from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict if the customer will subscribe (yes/no) to a term deposit (variable y)',
    author='Ahmad',
    license='',
)
